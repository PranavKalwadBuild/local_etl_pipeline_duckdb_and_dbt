apiVersion: v1
kind: Service
metadata:
  name: airflow-ingestion-webserver
  namespace: airflow-ingestion
spec:
  selector:
    app: airflow-ingestion-webserver
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
  type: ClusterIP
